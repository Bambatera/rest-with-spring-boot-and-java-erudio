cors:
    originPatterns: http://localhost:8080,http://localhost:3000,https://erudio.com.br
security:
    jwt:
        token:
            secret-key: 53cr37
            expire-length: 3600000
spring:
    datasource:
        driver-class-name: com.mysql.jdbc.Driver
        url: jdbc:mysql://localhost:3307/java-rest-by-erudio?useTimezone=true&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
        username: root
        password: L34ndr0.$1lv@
    jpa:
        open-in-view: false
        hibernate:
            ddl-auto: none
        show-sql: false
#        properties:
#            hibernate:
#                dialect: org.hibernate.dialect.MySQLDialect
spring-doc:
    pathsToMatch: /api/**/v1/**
    swagger-ui:
        use-root-path: true